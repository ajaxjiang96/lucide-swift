//
//  Lucide.swift
//  LucideSwift
//
//  Auto-generated type-safe enum for all Lucide icons
//  This file is generated by the LucideGenerator tool
//  Do not edit manually
//

import SwiftUI

/// A Shape representing a Lucide icon
public struct LucideShape: Shape {
    let path: Path
    
    public init(path: Path) {
        self.path = path
    }
    
    public func path(in rect: CGRect) -> Path {
        // Scale the pre-generated path to fit the rect
        // Lucide icons are designed for 24x24 viewBox
        let scaleX = rect.width / 24
        let scaleY = rect.height / 24
        
        var transform = CGAffineTransform.identity
        transform = transform.translatedBy(x: rect.minX, y: rect.minY)
        transform = transform.scaledBy(x: scaleX, y: scaleY)
        
        return path.applying(transform)
    }
}

// Array extension for safe access
private extension Array {
    subscript(safe index: Int) -> Element? {
        return indices.contains(index) ? self[index] : nil
    }
}
